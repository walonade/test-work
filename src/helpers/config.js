const sliderSettings = {
  dots: false,
  infinite: true,
  variableWidth: true,
  arrows: false,
  speed: 500,
  slidesToShow: 10,
  slidesToScroll: 1
};

const masonryOptions = {
  transitionDuration: "0.6s",
  gutter: 30,
  fitWidth: true
};

export { sliderSettings, masonryOptions };
